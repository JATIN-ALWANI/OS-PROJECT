#include<stdio.h>

int main()
{
      int burst_time[10], process[10], waiting_time[10], turnaround_time[10], priority[10],arrival_time[10],waiting_time[10];
      int i, j, proc, sum = 0, position, temp;
      float average_wait_time, average_turnaround_time;
      printf("Enter Total Number of Processes:\t");
      scanf("%d", &proc);
      printf("\nEnter Burst Time and Priority For %d Processes\n", limit);
      for(i = 0; i < proc; i++)
      {
            printf("\nProcess[%d]\n", i + 1);
            
            printf("Process Burst Time:\t");
            scanf("%d", &burst_time[i]);
            
            printf("Enter Arrival time:\t");
            scanf("%d",&arrival_time[i]);
            
            printf("Enter waiting time as per Shortest Job First":\t);
            Scanf("%d",&waiting_time[i]);
            priority[i]=1+waiting_time[i]/burst_time[i];
            
            printf("Process Priority:\t %d",);
            scanf("%d", &priority[i]);
            
            process[i] = i + 1;
      }
